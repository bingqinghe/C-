关于修饰符

1. internal大体相当于C++的friendly,但是它是基于程序集的  
* 对
  错

2. protected是与继承相关的  
* 对
  错

3. static变量，即可以用类名来访问，又可以用对象实例来访问  
  对
* 错

4. static方法中可以用this来访问其成员  
  对
* 错

5. C#变量不能将全局变量写在类之外，但可以用static变量表示全局变量。  
  对
* 错

6. readonly就是const  
  对
* 错

7. const只能用于基本类型及string  
* 对
  错

8. sealed表示不能被继承  
* 对
  错

9. abstract表示抽象的，不能被实例化，也就是说不能有构造方法  
  对
* 错

10. abstract类一般都是用来被继承的  
* 对
  错
